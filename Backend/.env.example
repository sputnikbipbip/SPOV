ASPNETCORE_ENVIRONMENT=Development
ASPNETCORE_URLS=http://0.0.0.0:8080
# For production, use a strong password and never commit this file.
# For Docker, this file is loaded as env_file by compose.yaml.
ConnectionStrings__DefaultConnection=Host=localhost;Port=5432;Database=spov;Username=spov;Password=changeme
